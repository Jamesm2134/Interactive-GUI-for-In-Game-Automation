def afk_Clicker():
    seconds_Wait = input("How many seconds do you want to have the program wait before starting")
    time.sleep(int(seconds_Wait))

    while True:
        pyautogui.click(button='left')
        time.sleep(0.63)

        if keyboard.is_pressed('esc'):
            print("done")
            break
